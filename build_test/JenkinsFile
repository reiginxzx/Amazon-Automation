pipeline{

    agent{label 'Amazon-Search'}

    stages('test'){

        stage('test'){
            steps{
                sh "mvn clean compile test"
            }
        }

        stage('Build application'){
            steps{
                echo "mvn clean install -Dmaven.test.skip-true"
            }
        }

        stage('Create Docker image'){
            steps{
                echo "Creando Docker"
            }
        }


    }

}